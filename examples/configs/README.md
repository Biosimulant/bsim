Configs moved to `biomodels`
============================

The example wiring configs previously shipped in `bio-sim` were moved to the public companion repo:

- https://github.com/Biosimulant/biomodels/tree/main/spaces

To run a config file:

```bash
python -m bsim /path/to/wiring.yaml
```
